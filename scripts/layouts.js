document.addEventListener('DOMContentLoaded', function () {
    var socialPanel = document.querySelector('.socialPanel');
    // 绑定显示图标信息事件
    socialPanel.addEventListener('mouseover', function function_name (argument) {
         // body...  
    });
});